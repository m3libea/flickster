<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.m3libea.flickster">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="YOUTUBE_API_KEY" android:value="AIzaSyB9iob-xfu_6UKBKVcT1foyYpCWQ8fxgzQ" />

        <activity android:name=".activities.MovieActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MovieDetailActivity" />
        <activity android:name=".activities.TrailerActivity" android:screenOrientation="landscape" />
    </application>

</manifest>